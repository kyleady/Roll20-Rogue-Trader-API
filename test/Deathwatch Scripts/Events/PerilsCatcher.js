var expect = require('chai').expect;
var fs = require('fs');
var path = require('path');
require('mock20');
describe('perilsCatcher()', function() {
	it('should send a warning if a Wp check rolls a 9 in the Ones Location');
});
