var expect = require('chai').expect;
var fs = require('fs');
var path = require('path');
require('mock20');
describe('perilsCatcher()', function() {
	it('should store damage from Out of Date starship attacks');
});
